
//-----------------Map-------------------

"insert array".map(("insert element name for each item in array", "insert optional element for the index") => {
    "insert what to do with each item"
})

//-------------Splice--------------------

"insert array".splice("insert the index to start changing in the array", "insert the number of indexs to delete in the array from start point (0 if none)", "insert elements to be added to the array split with commas")